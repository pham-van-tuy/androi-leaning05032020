package com.example.playkotlinlang.ProductivityBossters

import android.os.Build
import androidx.annotation.RequiresApi
import java.time.chrono.ChronoLocalDate
import java.time.LocalDate

class SmartCasts {
    //    val dateNow : ChronoLocalDate = LocalDate.now()
    fun SmartCastsTest() {
//        println(dateNow)
//        if (dateNow != null && dateNow.isLeapYear){
//            println(dateNow.isLeapYear)
//        }
//        if (dateNow == null || !dateNow.isLeapYear) {         // 4
//            println("There's no Feb 29 this year...")
//        }
//        if (dateNow is LocalDate) {
//            val month = dateNow.monthValue                 // 5
//            println(month)
//        }


    }
}